package pt.isel.gomoku.ui.screens.board

import androidx.compose.ui.graphics.Color

val gridOpacity = 0.4F
//val gridColor = Color.White.copy(alpha = gridOpacity)
val gridColor = Color(250, 216, 127, 255).copy(alpha = gridOpacity)